Version 1.0 'Cold Mountains'

This is the first release version of HIGGERSutil (aka HiggUtil)
Base features:
- "changelog" command
- "uarand" (user agent randomizer) command (numbers/nums, words, offensive) (overrides "staticua")
- "useragent" (set user agent to static string) (overrides "uaran")
- "threads" command ('threads <threads to launch>')
- "target" command (<target ip>)
- "start" & "stop" commands (does what it says, starts or stops the attack with the settings applied)
- Time measurements (might be invalid)
- Sync running threads and target threads WHILE running

----------------------------------------------------